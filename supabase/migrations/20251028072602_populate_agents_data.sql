/*
  # Populate agents table with initial data

  1. Description
    - Populates the agents table with all agent records from the JSON data
    - Uses ON CONFLICT to handle existing records safely
    - Updates existing records with latest data
*/